api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
        jsonld: [ 'application/ld+json' ]
    swagger:
        versions: [3]
        api_keys:
            bearerAuth:
                name: Authorization
                type: header
